import LoadingModal from "@/components/modals/loading-modal";

/**
 * Root loading component displayed during top-level route transitions.
 * @returns loading modal component
 */
export default function RootLoading() {
  return <LoadingModal />;
}
